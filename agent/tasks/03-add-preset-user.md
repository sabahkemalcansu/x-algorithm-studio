# Task 03 — Add a preset user

Add `presets/users/nba_heavy.json` with a short history biased to NBA-style hints.

Document how a future run could load it (even if wiring is TODO).

Keep format similar to `fixtures/sample_results.json` → `user.history`.
